import { ModelsFilterPipe } from './models-filter.pipe';

describe('ModelsFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new ModelsFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
